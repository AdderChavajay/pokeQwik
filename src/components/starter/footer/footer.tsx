import { component$ } from "@builder.io/qwik";

export default component$(() => {
  return (
    <footer>
      <div class="w-full">
        <h2 class="font-semibold text-2xl text-center">Footer</h2>
      </div>
    </footer>
  );
});
